# 小嘉物联网平台 UI 设计规范

> 本规范旨在统一项目视觉风格，确保开发的界面保持一致的科技感与专业性。

## 🎨 整体风格

- **主题色**：深蓝色系（`#0a1b3e`, `#003366`, `#0066cc`）
- **辅助色**：亮蓝（`#00ccff`）、白色（`#ffffff`）、红色（`#cc0000`）
- **字体**：`Microsoft YaHei` 或 `Segoe UI`
- **图标**：使用扁平化矢量图标（建议用 Font Awesome 或 Material Icons）

---

## 🖼️ 界面元素规范

| 元素 | 规范 |
|------|------|
| **窗口背景** | `#0a1b3e`（深海蓝） |
| **标题栏** | `#003366`，带 `#0099ff` 下边框 |
| **卡片容器** | 背景 `#112a50`，边框 `#0066cc`，圆角 `8px` |
| **按钮** | `#0066cc` → `#004d99`（悬停）→ `#003366`（点击） |
| **文字颜色** | 主要信息：`#e0f7ff`；标题：`#00ccff`；错误：`#ff6666` |

---

## 🔧 组件使用说明

### 1. 使用 `BaseWindow` 基类

所有窗口必须继承 `BaseWindow`，例如：

```python
from ui.base_window import BaseWindow

class AnalyzerWindow(BaseWindow):
    def __init__(self):
        super().__init__("小嘉 - 数据分析")
        # 使用 self.main_layout 添加组件
        self.main_layout.addWidget(self.create_card("温度趋势", "当前平均温度：25.3°C"))
```

### 2. 创建数据卡片

使用 `create_card(title, content)` 快速生成标准化卡片：

```python
card = self.create_card("设备在线率", "85% (43/50)")
self.main_layout.addWidget(card)
```

### 3. 添加状态栏

在界面底部显示运行状态：

```python
self.add_status_bar("MQTT 已连接，正在接收数据...")
```

